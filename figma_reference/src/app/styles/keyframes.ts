export const APP_KEYFRAMES = `
  @keyframes floatNote { from{transform:translateY(0) rotate(-5deg);opacity:.55} to{transform:translateY(-10px) rotate(5deg);opacity:0} }
  @keyframes countPop  { from{transform:scale(1.5);opacity:0} to{transform:scale(1);opacity:1} }
  @keyframes fadeIn    { from{opacity:0} to{opacity:1} }
  @keyframes bar       { from{height:4px} to{height:18px} }
  input::placeholder   { color:rgba(139,92,246,0.4) }
  ::-webkit-scrollbar  { display:none }
`;
